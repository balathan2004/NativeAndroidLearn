./gradlew assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk


./gradlew assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk && adb logcat -s MainActivity

for logs = adb logcat -s MainActivity
